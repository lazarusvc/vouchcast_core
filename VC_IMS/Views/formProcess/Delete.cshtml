@model VC_IMS.Models.VC_formProcess

@{
    ViewData["Title"] = "Delete";
}

<h6 style="color:red;">Are you sure you want to delete this?</h6>
<div class="card basic-data-table">
    <div class="card-header">
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-danger" />
        </form>
    </div>
    <div class="card-body" style="overflow-y:scroll; max-height: 250px;">
        <div>
         <dl class="row">
        <dt class="col-sm-2">Label</dt>
        <dd class="col-sm-10">@Model.name</dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.url).ToUpper():
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.url)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.VC_formsId).ToUpper():
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.VC_formsId)
        </dd>
    </dl>
        </div>
    </div>
</div>
