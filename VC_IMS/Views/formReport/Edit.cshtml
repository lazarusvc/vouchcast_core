@model VC_IMS.Models.VC_formReport

@{
    ViewData["Title"] = "VC_formReport | EDIT";
}
<hr />
<div class="row">
<div class="col-md-12">
<div class="card">
<div class="card-header">
<a asp-action="Index" class="btn btn-info">Home</a>
</div>
<div class="card-body" style="overflow-y:scroll; max-height: 250px;">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="row mb-24 gy-3 align-items-center">
                <label asp-for="url"  class="form-label mb-0 col-sm-4"></label>
                <div class="col-sm-10">
                <input asp-for="url" class="form-control" />
                <span asp-validation-for="url" class="text-danger"></span>
                </div>
            </div>
            <div class="row mb-24 gy-3 align-items-center">
                <label asp-for="VC_formsId"  class="form-label mb-0 col-sm-4"></label>
                <div class="col-sm-10">
                <select asp-for="VC_formsId" class="form-control" asp-items="ViewBag.VC_formsId"></select>
                <span asp-validation-for="VC_formsId" class="text-danger"></span>
                </div>
            </div>
                <input type="submit" value="Save" class="btn btn-primary" />
        </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
